[package]
name = "isdbt-wasm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "ISDB-T 1セグ復調器の WASM バインディング（ブラウザで IQ→MPEG-TS）"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
isdbt-dsp = { path = "../isdbt-dsp" }
wasm-bindgen = "0.2"
